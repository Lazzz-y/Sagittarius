<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 设置特定包/类的日志级别 -->
    <!-- 例如：设置测试类所在包的日志级别为DEBUG -->
    <logger name="io.github.lazzz.sagittarius.common.redisson.service.impl" level="DEBUG"/>
    <!-- 全局默认日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>